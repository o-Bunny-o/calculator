/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/calculator", 
    assetPrefix: "/calculator/",    
    trailingSlash: true,
};

export default nextConfig;
